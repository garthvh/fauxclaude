# build output — rebuild with mac-app/build-app.sh
FauxClaude.app/
OllamaClaudeShim.app/

# windows-app build output — rebuild with `dotnet publish`
windows-app/bin/
windows-app/obj/

# runtime artifacts
shim.log
*.log
.DS_Store

# optional PDF-vision dependency (npm install pdf-to-img to enable the fallback
# rasterizer) — poppler is the primary path and needs no npm install at all
node_modules/
